spring.application.name=user-service

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Logging
logging.level.org.springframework.security=INFO
logging.level.project.semester.aspm.userservice=INFO
